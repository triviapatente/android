<?xml version="1.0" encoding="utf-8"?>

<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".app.views.game_page.round_details.RoundDetailsActivity"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:background="@android:color/white"
        app:behavior_hideable="true"
        app:behavior_peekHeight="0dp"
        android:id="@+id/quizDetailLayout"
        android:layout_height="@dimen/sheet_min_height"
        app:layout_behavior="android.support.design.widget.BottomSheetBehavior">

        <ImageView
            android:layout_width="@dimen/quiz_image_size_big"
            android:layout_height="@dimen/quiz_image_size_big"
            android:id="@+id/quizDetailImage"
            android:layout_gravity="center"/>

        <TextView
            android:id="@+id/quizDetailTextView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:layout_marginRight="@dimen/sheet_margin"
            android:layout_marginEnd="@dimen/sheet_margin"
            android:layout_marginLeft="@dimen/sheet_margin"
            android:layout_marginStart="@dimen/sheet_margin"
            android:textColor="@color/quizTextColor" />

    </LinearLayout>
</android.support.design.widget.CoordinatorLayout>